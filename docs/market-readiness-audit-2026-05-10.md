# Rift Road Market-Readiness Audit - 2026-05-10

## Objective Restated

Make `Rift Road: Beasts of the Afterglow` meaningfully marketable by proving these deliverables:

1. A playable Stage 1 vertical slice.
2. A production-deployable macOS build path.
3. Real launched-game screenshot and playtest evidence.
4. Current validation results.
5. A candid market-readiness assessment.
6. Evidence that supports public playtest or release-candidate status before calling the goal complete.
7. Player love or commercial viability backed by player or market evidence before claiming the game can earn money.

The 2026-05-11 replacement drafts in `docs/game_spec.md` and `docs/game-story.md` sharpen the internal bar: the first success condition is a Stage 1 slice that feels good, looks alive, and is satisfying to play on the target M1 iMac. Market-facing decisions come after that fun/feel proof, not before it.

## Prompt-To-Artifact Checklist

| Requirement | Current evidence | Status | Gap |
| --- | --- | --- | --- |
| Playable Stage 1 vertical slice | `src/wildcoil/tools/runtime_test_runner.gd` covers title flow, Stage 1 flow, autoplay, hero preview, restart, controller title flow, controller hot-plug status, keyboard fallback title -> hero select -> preview cancel -> Stage 1 plus movement/attack/jump/special/dash/pause, focus-loss pause/resume with audio-manager suspend/resume state, opening story panels and barks, road-collapse resume, Brask intro/phase/escape story beats, pickup health/luma clarity, Stage Clear score/rank summary, and return-to-title. `bash scripts/check.sh` passed with the current test suite and Godot runtime smoke. `bash scripts/smoke_exported_macos_app.sh` now captures title, hero-select, opening story, gameplay, post-intro combat, pickup clarity, road-collapse, Brask intro, Stage Clear, Game Over/retry, and post-retry gameplay viewports from the launched exported app, including `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-opening-story.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-combat.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-pickups.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-road-collapse.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-brask-intro.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-stage-clear.png`, `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-game-over.png`, and `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-retry-gameplay.png`. `bash scripts/smoke_exported_keyboard_fallback.sh` records automated keyboard input proof from the launched exported app in `docs/playtest-captures/keyboard-fallback-latest/stage1-exported-app-keyboard-fallback.json` and `stage1-exported-app-keyboard-fallback.png`. `bash scripts/smoke_exported_focus_resume.sh` records automated exported-app focus/resume proof in `docs/playtest-captures/focus-resume-latest/stage1-exported-app-focus-resume.json` and `stage1-exported-app-focus-resume.png`. | Partially proven | Runtime and launched-app smoke tests are strong, but the 2026-05-11 spec still requires a full recorded human playthrough from title to Stage Clear or fail/retry plus manual audible focus-loss confirmation in the exported app. |
| Stage 1 visual/UI polish toward north-star images and 2026-05-11 spec | Proof captures exist in `docs/playtest-captures/`, including exported-app title/gameplay/manual-input screenshots, combat impact, boss HUD, cinematic banner, and stage ruins/readability captures. The refreshed exported-app title capture shows a branded Rift Road logo lockup and start plate instead of plain centered text. The opening story capture `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-opening-story.png` shows a short Raya/Nika story panel that explains drill marks, cages, and route stakes from the launched exported app. The refreshed gameplay capture shows the Stage 1 intro banner as a centered 760px by 58px strap above the combatants, preserving more of the combat lane and background silhouettes than the earlier broad overlay, and the strap now reads `Free the transport cages` without ellipsizing the full scenario objective. The refreshed post-intro combat capture `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-combat.png` gives a less obstructed launched-game view of the running fight after the intro strap has faded, keeps the objective rail to a single readable line, and no longer carries stale centered wave notice text over the sky. The pickup clarity capture `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-pickups.png` shows distinct health and luma/meter pickup treatments from the launched exported app. The road-collapse smoke capture `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-road-collapse.png` shows the luma-fracture/service-lane set piece between the opening wave and the next combat beat. The Brask intro capture `docs/playtest-captures/exported-app-smoke-latest/stage1-exported-app-smoke-brask-intro.png` shows the boss story banner from the launched exported app. The refreshed gameplay capture also shows a slimmer top HUD with smaller player/portrait/score panels and lighter objective treatment while retaining health, special, score, luma, objective, combo, portrait, and meter information. The refreshed hero-select smoke capture shows canted arcade cards, selected-card glow, portrait wells, planned-hero silhouettes, stat pips, and a canted arcade header/ribbon instead of flat prototype panels and plain floating heading text. The refreshed Stage Clear capture shows a canted arcade result frame with rank, score, luma, health, wrapped story, and next-stage text. The Game Over/retry smoke capture uses the same framed result treatment for the fail-state prompt. The post-retry gameplay capture proves the launched app can return from that result state to a Stage 1 gameplay viewport. | Partially proven | Current visuals improve over prototype, but they do not fully match `docs/design-docs/assets/stage1-visual-north-star.png`, `docs/design-docs/assets/stage1-background-north-star.png`, or the updated spec's HD 2D animated slice target; final production sprites/background/audio are still placeholders. |
| Production-deployable macOS build path | `scripts/package_macos.sh` exports `build/macos/Rift Road.zip`; current zip exists at 73 MB; `scripts/check_macos_signing_env.sh` reports the current signing/notary prerequisites are blocked; `docs/macos_release_inputs.example.env` provides a redacted template for the required local signing preflight inputs; `scripts/sign_notarize_macos.sh` now defines the guarded path for signing with Developer ID, submitting through `xcrun notarytool`, stapling, Gatekeeper-checking, and writing `build/macos/Rift Road-signed-notarized.zip`; `scripts/audit_macos_package.sh` verifies structure and reports `RIFT_ROAD_PACKAGE_AUDIT internal-only`; `scripts/smoke_exported_macos_app.sh` extracts and launches the zip for local exported-app viewport proof. `bash scripts/check_second_machine_evidence.sh` now requires clean-machine proof under `docs/playtest-captures/second-machine-latest/`, and `bash scripts/collect_second_machine_evidence.sh` is bundled to generate that proof on the actual second Mac. | Incomplete | Bundle signature verifies, but the audit reports no Developer ID authority, empty signing identity, empty Apple Team ID, disabled notarization, Gatekeeper rejection, no stapled ticket, and no second-machine verification. The release signing script currently blocks without real Apple signing credentials and has not produced a successful notarized artifact. The smoke script and collector prove process readiness, not production distribution until a real signed/notarized artifact and second Mac record accepted evidence. |
| Real launched-game screenshots | Fresh exported app captures exist: `stage1-exported-app-title-window.png`, `stage1-exported-app-gameplay-window.png`, `stage1-exported-app-manual-input-window.png`, and repeatable title/hero-select/opening-story/gameplay/post-intro-combat/pickups/road-collapse/Stage Clear/Game Over/post-retry gameplay smoke captures under `docs/playtest-captures/exported-app-smoke-latest/`, including `stage1-exported-app-smoke-opening-story.png`, `stage1-exported-app-smoke-combat.png`, `stage1-exported-app-smoke-pickups.png`, `stage1-exported-app-smoke-road-collapse.png`, `stage1-exported-app-smoke-stage-clear.png`, `stage1-exported-app-smoke-game-over.png`, and `stage1-exported-app-smoke-retry-gameplay.png`. | Proven for screenshots | App-window capture is valid evidence for local launch, but the current screenshot set is not a human playthrough or combat-feel recording. |
| Manual playtest evidence | Codex launched the extracted exported app through LaunchServices, activated by bundle id, advanced title -> hero select -> Stage 1, and sent movement/attack key input; the smoke script repeats the title-to-hero-select-to-stage path locally; `docs/public_playtest_gate.md` defines the external session protocol and evidence thresholds. `bash scripts/prepare_known_tester_packet.sh` now bundles `docs/controller_validation.md` and `docs/second_machine_validation.md` so supervised sessions carry the manual-gate checklists, and it records manual gate statuses/logs for focus/audio, playtest, controller, and second-machine evidence. `bash scripts/collect_controller_evidence.sh` now generates manual controller/keyboard evidence notes and paste-ready snippets after real exported-app sessions. `bash scripts/collect_playtest_evidence.sh` now generates external-session notes and paste-ready `docs/playtest_log.md` rows after real tester sessions. `bash scripts/check_playtest_evidence.sh` now blocks public-playtest-candidate status until the session log has at least 5 completed rows, one second-Mac row, two controller-family rows, and majority replay intent. `bash scripts/check_controller_evidence.sh` now blocks until two physical controller-family sessions and keyboard fallback record detailed control coverage with non-placeholder build/device/evidence metadata and real non-empty capture files. | Partial | Needs human tester sessions, observation notes, and ideally video from title through at least one clear/fail/retry loop. |
| Validation results | `bash scripts/check.sh` passed with 104 tests and Godot runtime smoke after the manual gate status packet pass. `python3 -m pytest tests/test_runtime_smoke.py::test_controller_hotplug_status_updates_ui_and_runtime_state -q` proves the headless controller hot-plug smoke emits `RIFT_ROAD_CONTROLLER_HOTPLUG connected=true disconnected=true prompt=true events=2`. `python3 -m pytest tests/test_runtime_smoke.py::test_keyboard_fallback_title_to_stage_and_action_flow -q` proves the headless keyboard fallback smoke emits `RIFT_ROAD_KEYBOARD_FALLBACK title=true hero_select=true movement=true attack=true jump=true special=true dash=true pause=true cancel=true`. `python3 -m pytest tests/test_runtime_smoke.py::test_stage_one_focus_loss_pauses_and_resumes -q` proves the headless focus smoke emits `RIFT_ROAD_FOCUS_RESUME focus_pause=true overlay=true audio=true resume=true`, where `audio=true` verifies audio-manager focus suspend/resume state in automation rather than audible device output. `bash scripts/package_macos.sh` regenerated `build/macos/Rift Road.zip`. `bash scripts/check_macos_signing_env.sh` reports `RIFT_ROAD_SIGNING_PREFLIGHT blocked`. `bash scripts/sign_notarize_macos.sh --help` documents the required release inputs, and `bash scripts/sign_notarize_macos.sh` reports `RIFT_ROAD_RELEASE_SIGNING blocked` when those inputs are missing. `bash scripts/audit_macos_package.sh` reports the build is internal-only. `bash scripts/collect_controller_evidence.sh --help` documents the manual controller/keyboard evidence collector, and `bash scripts/collect_controller_evidence.sh` reports `RIFT_ROAD_CONTROLLER_EVIDENCE_COLLECTOR blocked` until a real session supplies the required session type, blocker note, and per-control confirmations. `bash scripts/collect_playtest_evidence.sh --help` documents the manual external-session collector, and `bash scripts/collect_playtest_evidence.sh` reports `RIFT_ROAD_PLAYTEST_COLLECTOR blocked` until a real session supplies the required tester, setup, input, timing, replay, confusion, cheap-damage, quote, and follow-up fields. `bash scripts/check_focus_audio_evidence.sh` currently reports `RIFT_ROAD_FOCUS_AUDIO_EVIDENCE blocked` because no manual audible focus-loss/resume session has been recorded. `bash scripts/smoke_exported_macos_app.sh` reports `RIFT_ROAD_EXPORTED_APP_SMOKE ok` and refreshed title, hero-select, opening story, gameplay, post-intro combat, pickup clarity, road-collapse, Brask intro, Stage Clear, Game Over/retry, and post-retry gameplay captures from the launched exported app. `bash scripts/smoke_exported_keyboard_fallback.sh` reports `RIFT_ROAD_EXPORTED_KEYBOARD_FALLBACK ok` when the launched exported app writes a JSON checklist and viewport capture for title, hero select, movement, attack, jump, special, dash, pause, and cancel/back. `bash scripts/smoke_exported_focus_resume.sh` reports `RIFT_ROAD_EXPORTED_FOCUS_RESUME ok` when the launched exported app writes focus-pause, overlay, audio-manager, return-focus, resume, and viewport evidence. `bash scripts/prepare_known_tester_packet.sh` reports `RIFT_ROAD_KNOWN_TESTER_PACKET internal-only` and includes controller and second-machine validation checklists, keyboard fallback evidence, focus/resume evidence, build commit, package SHA-256, and manual evidence gate statuses/logs in the packet manifest. `bash scripts/sample_exported_app_performance.sh` records launched-app steady-state frame timing on local Apple Silicon Mac A (`arm64`, `Apple M1`, `iMac21,2`; see `docs/playtest-captures/performance-host-latest/host-profile.md`); latest local 1280x720 windowed result is `avg_ms=12.523`, `max_ms=30.123` after excluding 8 startup/render warmup frames, a local 1920x1080 windowed result is `avg_ms=3.199`, `max_ms=6.652`, and a local fullscreen exported-app result is `avg_ms=1.583`, `max_ms=2.793`, `window_mode=fullscreen`. `bash scripts/check_second_machine_evidence.sh` currently reports `RIFT_ROAD_SECOND_MACHINE_EVIDENCE blocked` because no second-machine proof files have been recorded. `bash scripts/check_controller_evidence.sh` currently reports `RIFT_ROAD_CONTROLLER_EVIDENCE blocked` because no physical controller sessions have been recorded and no manual exported-app keyboard fallback row has been recorded; placeholder metadata and missing evidence capture files in marked rows are rejected by regression coverage. `bash scripts/check_playtest_evidence.sh` currently reports `RIFT_ROAD_PLAYTEST_EVIDENCE blocked` because no external session rows have been recorded. `bash scripts/check_release_candidate.sh` combines those automated gates, including exported-app keyboard fallback, focus/resume smoke, and manual focus/audio evidence, and writes `build/release-gate/latest/completion-audit.md` with a prompt-to-artifact checklist before currently reporting `RIFT_ROAD_RELEASE_GATE blocked`. Simulated controller title/pause flow, automated controller hot-plug status, automated keyboard fallback action flow, automated focus-loss/resume, automated launched-app keyboard fallback flow, and automated launched-app focus/resume flow are covered by smoke modes. `stage1_performance_sample` emits `RIFT_ROAD_PERF stage1`; latest local result is `avg_ms=16.726`, `max_ms=40.161`. | Current | Validation does not cover physical controller devices, manual exported-app keyboard fallback, a second Apple Silicon Mac, second-machine install, a successful signed/notarized external distribution flow, or real external tester sessions. |
| Candid market-readiness assessment | `docs/playtest-captures/stage1-marketability-handoff-2026-05-10.md` documents what works and what remains placeholder. | Present | This audit should remain updated after every public-playtest readiness pass. |
| Public playtest or release-candidate proof | `docs/public_playtest_gate.md` now defines tester instructions, preflight commands, evidence thresholds, and no-claim rules. No external tester distribution evidence, no tester feedback set, no second-machine install, no signing/notarization. | Not achieved | Public playtest readiness requires packaging, signing/notarization decision, at least one clean-machine install, and recorded playtest feedback. |
| Player love / commercial viability | `docs/public_playtest_gate.md` defines the required evidence for replay intent, willingness to pay, and repeated player reactions. No external player reactions, retention evidence, wishlist/conversion data, or market test exist yet. | Not achieved | Cannot claim players will love it or that it can earn money. Need tester sessions and market-facing page/trailer/key-art validation. |

## Completion Decision

The active goal is not complete.

The current build is a stronger internal vertical slice than before: it launches from an exported macOS app, has a rebuilt package, starts Stage 1 through real input, has a more readable opening wave, has a less placeholder-like title screen, has data-driven opening story panels and in-stage Raya/Nika barks with a launched opening-story capture, has a centered compact Stage 1 intro strap with non-ellipsized `Free the transport cages` display copy, has a slimmer top HUD footprint, has a one-line combat objective rail, clears stale wave notice text before the post-intro combat proof, has a repeatable post-intro launched combat capture after the strap fades, has data-driven health and luma/meter pickups with a launched pickup clarity capture, has a road-collapse set piece and exported-app capture between the opening fight and service-lane wave, has Brask intro/phase/escape story beats with a launched Brask intro capture, has less placeholder-like hero-select cards plus a canted hero-select header/ribbon, has framed Stage Clear and Game Over/retry result screens, has a Stage Clear rank/score/luma/health summary, has repeatable post-retry launched gameplay proof, and passes the current automated validation suite. The 2026-05-11 spec/story update still raises the next bar from incremental presentation polish to externally verified Stage 1 feel validation. That is meaningful progress toward marketability, but it is not enough to claim production readiness, player love, or commercial viability.

## Next Required Gates

1. Physically test at least two controller families and keyboard fallback on the exported macOS app.
2. Resolve the macOS package audit warnings: Developer ID authority, Apple Team ID, notarization, Gatekeeper acceptance, stapled ticket, and second-machine install.
3. Run the `docs/public_playtest_gate.md` protocol and capture recorded human playtests from title through Stage 1 clear/fail/retry, with notes about confusion, unfair damage, delight, and replay intent.
4. Capture a second Apple Silicon Mac performance/install sample; local Apple Silicon Mac A has 1280x720 windowed, 1920x1080 windowed, and 1920x1080 fullscreen samples, but that is not second-machine proof.
5. Replace or clearly scope remaining placeholder art/audio before any market-facing claim.
